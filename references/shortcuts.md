# 达芬奇 21 键盘快捷键（从 21.1 官方手册蒸馏）

> 数据源：**DaVinci Resolve 21.1 官方参考手册**（原文汉化 @谜一样的剪辑师，收集整合 @一个成熟的剪辑猿）。
> 本表由手册全文（4351 页）自动挖掘，共 345 条。手册以 macOS 惯例展示（Command / Option）；
> Windows / Linux 用 **Ctrl** 替 Command、**Alt** 替 Option。括号内 P 数字为手册出处页码。

模块：通用 / 媒体 / 剪辑 / 快编 / Fusion / 调色 / Fairlight / 交付 / 照片 / 其他

## 通用 / 媒体（15 条）

- **Option-Command-G** — • 选择 调色 > 静帧 > 抓取静帧（Option‑Command‑G）。 `P11`
- **Command-F** — 您也可以通过选择“工作区 > 检视器模式 > 影院检视器”(Command‑F)，将检视器切换为影院检视器 `P23`
- **Command-F** — 您也可以选择“Workspace > Viewer Mode > Cinema Viewer (Command‑F)”， `P40`
- **Option-F** — (Option‑F) 和 Full Screen Viewer (Shift‑F)，可为您提供更大的工作区域， `P40`
- **Shift-F** — (Option‑F) 和 Full Screen Viewer (Shift‑F)，可为您提供更大的工作区域， `P40`
- **Command-F** — 工作区 > 检视器模式 > Cinema Viewer”(Command‑F)，将检视器切换到 Cinema Viewer 模式，从 `P51`
- **Return 键** — 双击包含大多数数值的字段，会高亮显示该数值，方便您使用键盘键入新的数值，并按 Return 键确认更改。 `P63`
- **Command-C** — 右键点按源字段，并从下拉菜单中选择“复制”（Command‑C）。 `P64`
- **Command-V** — 右键点按目标字段，并从下拉菜单中选择“粘贴”（Command‑V）。 `P64`
- **Command-Z** — • 选择编辑 > 撤销（Command‑Z）以撤销上一步更改。 `P69`
- **Shift-Command-Z** — • 选择编辑 > 重做（Shift‑Command‑Z）以重做到下一步更改。 `P69`
- **Backspace 键** — • 删除工程： 选择一个或多个工程，然后按 Backspace 键，或右键单击所选工程之一并选择“删除”。 `P75`
- **Command-S** — • 选择文件 > 保存项目（Command‑S）。 `P85`
- **Command-Shift-S** — 要将项目当前状态以新名称另存为副本，请选择文件 > 项目另存为（Command‑Shift‑S），然后 `P85`
- **Option-Command-S** — • 选择文件 > 创建时间线备份（Option‑Command‑S）。 `P89`

## 剪辑 / 快编（190 条）

- **空格 键** — 并监控您的监视文件夹。如果您想随时停止该过程，只需选择“停止”按钮。您也可以按空格键切 `P211`
- **Command-R** — 选择调色 > Resolve Live（Command‑R）。 `P307`
- **Command-R** — • 选择调色 > Resolve Live（Command‑R）。 `P308`
- **Shift-Command-R** — • 选择调色 > Resolve Live 冻结（Shift‑Command‑R）。 `P308`
- **Command-Option-R** — • 选择调色 > Resolve Live 快照（Command‑Option‑R）。 `P308`
- **回车 键** — 使用向上和向下方向键选择您要使用的变量，然后按回车键选择添加该变量。 `P336`
- **回车 键** — 当您在一个栏位中添加一个或多个元数据变量并按回车键后，该字符串就会被替换为相应的文本。 `P336`
- **Delete 键** — • 在使用变量的栏位内点击以开始编辑，点击某个变量将其选中，然后按 Delete 键。 `P336`
- **回车 键** — 输入字符的关键词。使用向上和向下方向键在列表中选择您要使用的关键词，然后按回车键选 `P337`
- **回车 键** — • 双击任意关键词使其可编辑，然后像编辑其他文本一样进行修改，并按回车键使其重新成为图形关键词标签。 `P337`
- **Delete 键** — • 点击任意关键词将其选中，然后按 Delete 键。 `P337`
- **回车 键** — 在文本栏位中输入新关键词，然后按回车键。 `P339`
- **Command-F** — 您还可以通过选择工作区 > 检视器模式 > 影院检视器 (Command‑F) 将检视器置于影院检视器模式， `P349`
- **Command-Z** — • 选择编辑 > 撤销 (Command‑Z) 以撤销上一步更改。 `P358`
- **Shift-Command-Z** — • 选择编辑 > 重做 (Shift‑Command‑Z) 以重做到下一步更改。 `P358`
- **Return 键** — 显示的状态下，输入新名称并按 Return 键。或者，您也可以右键单击媒体夹，选择重命名媒体夹， `P377`
- **Return 键** — 然后输入新名称并按 Return 键。 `P377`
- **Shift-Command-O** — 选择文件 > 导出 > 导出 AAF、XML、DRT（Shift‑Command‑O）。 `P379`
- **Shift-Command-I** — • 选择文件 > 导入时间线 > 导入 AAF、XML、DRT（Shift‑Command‑I），然后在文件系统对话 `P379`
- **Command-W** — • 关闭标签：关闭当前标签（Command‑W）（Windows 和 Linux 上为 CTL‑W）。 `P393`
- **Return 键** — 刚输入字符的关键词。使用向上和向下方向键在列表中选择要使用的关键词，然后按 Return 键选 `P408`
- **Return 键** — • 双击任意关键词使其可编辑，然后像编辑任何其他文本一样编辑它，并按 Return 键使其再次成为图形关键词标签。 `P409`
- **Delete 键** — • 单击任意关键词以将其选中，然后按 Delete 键。 `P409`
- **回车 键** — 名称，然后按回车键接受该名称。 `P424`
- **回车 键** — 使用向上和向下箭头键选择您要使用的变量，然后按回车键选择添加该变量。 `P425`
- **回车 键** — 一旦您向片段的片段名称列添加一个或多个元数据变量并按回车键，该字符串就会被其对应的文 `P425`
- **Delete 键** — • 单击使用变量的字段内部以开始编辑，单击一个变量将其选中，然后按 Delete 键。 `P426`
- **回车 键** — 自动选择下一个未整理的片段： 勾选此框后，当您在输入元数据字段后按回车键时，媒体池中的 `P436`
- **Shift-Command-I** — • 在任何页面中，选择文件 > 导入时间线（Shift‑Command‑I）。 `P506`
- **Shift-Command-I** — • 在任意页面中，选择文件 > 导入时间线（Shift‑Command‑I）。 `P512`
- **Command-Shift-I** — 使用文件 > 导入时间线 > 导入 AAF、EDL、XML 命令（Command‑Shift‑I），然后选择一个 Pro Tools `P521`
- **Shift-Command-I** — • 在任何页面中，选择文件 > 导入 AAF、EDL、XML（Shift‑Command‑I）。 `P526`
- **Shift-Command-I** — • 在任意页面中，选择 文件 > 导入时间线（Shift‑Command‑I）。 `P531`
- **Command-\** — 时间线 > 拆分片段（Command‑\）。 `P533`
- **Shift-D** — 应用的调色和特效。点击该按钮（Shift‑D）可禁用或重新启用调色和特效，或右键点击同一按钮访问 `P602`
- **回车 键** — • 自动选择下一张照片： 勾选此复选框后，当您在输入元数据字段后按回车键时，将选中媒体池中 `P611`
- **Command-R** — • 重置照片：（Command‑R）重置在照片标签中所做的任何调整。 `P611`
- **Shift-R** — • 移除所有照片特效：（Shift‑R）移除已应用到图像的任何特效。 `P611`
- **Shift-Command-R** — • 移除并重置全部：（Shift‑Command‑R）移除所有已应用的特效，并将图像重置回原始状态。 `P611`
- **Option-V** — 选择编辑 > 粘贴属性（Option‑V），或右键点击所选片段之一，然后从上下文菜单中选择粘贴属性。 `P612`
- **Option-V** — 转到要粘贴该特效的照片，然后右键单击，并在上下文菜单中选择粘贴属性（Option‑V）。 `P623`
- **Command-/** — 下方向键）和循环播放（Command‑/）按钮构成的控件，可点击控制片段和时间线的播放。每个按 `P640`
- **Shift-D** — 特效，以提升低功耗电脑上的播放性能。单击该按钮（Shift‑D）可禁用或重新启用调色和特效，或右 `P642`
- **Command-Z** — • 选择 编辑 > 撤销（Command‑Z）以撤销上一步更改。 `P651`
- **Shift-Command-Z** — • 选择 编辑 > 重做（Shift‑Command‑Z）以重做到下一步更改。 `P651`
- **Shift-Command-N** — 您可以通过选择 文件 > 新建媒体夹（Shift‑Command‑N），或右键点击媒体池并从上下文菜单中选择 `P655`
- **Shift-Command-O** — 选择 文件 > 导出 > 导出 AAF、XML、DRT（Shift‑Command‑O）。 `P657`
- **Shift-Command-I** — • 选择 文件 > 导入时间线 > 导入 AAF、XML、DRT（Shift‑Command‑I），然后在文件系统对 `P657`
- **Command-N** — 您也可以通过选择文件 > 新建时间线（Command‑N），或在媒体池空白处右键单击并选择创建新时 `P678`
- **Shift-D** — 色和特效，以提高低性能计算机上的播放性能。点击该按钮 (Shift‑D) 可禁用或重新启用调色和特效， `P687`
- **Shift-Command-W** — (Shift‑Command‑W) 在快编页面中使用 DaVinci Resolve 的全套示波器。 `P689`
- **Command-D** — 您可以在时间线上选择一个或多个片段，然后选择 片段 > 更改片段时长（Command‑D），或右键点 `P693`
- **Escape 键** — 图标，或直接按 Escape 键。 `P709`
- **空格 键** — • 勾选后，播放头固定在时间线中央，您的已编辑片段会在您播放（按空格键）、点动或穿梭（使 `P716`
- **空格 键** — • 取消勾选后，播放头会在您播放（按空格键）、点动或穿梭（使用 JKL 键）时向任一方向在片段 `P716`
- **N 键** — 间线中拖动片段的过程中，或使用指针擦动播放头时，可以按 N 键临时开启或关闭吸附。 `P717`
- **M 键** — • 添加标记： 将播放头移动到您想要标记的帧，然后单击时间线头部中的标记按钮，或按 M 键。您 `P718`
- **Return 键** — 标记对话框打开后，编辑标记的名称、备注字段、关键词字段或颜色，然后按 Return 键或单击完成 `P718`
- **Delete 键** — • 删除标记： 选择一个标记，然后按 Delete 键。或者，将播放头与某个标记对齐，然后按 `P718`
- **Command-A** — • 选择所有片段： 选择一个片段，然后选择 编辑 > 全选（Command‑A）。 `P719`
- **Option-Shift-V** — 头下方的所有片段（Option‑Shift‑V）一次选中播放头下方的所有片段。 `P719`
- **V 键** — • 使用键盘选择编辑点：按 V 键选择最接近播放头的编辑 `P720`
- **U 键** — 按 U 键可在选择出半部分、入半部分或整个编辑点之间切换。 `P720`
- **Delete 键** — 如果您想要从时间线中彻底移除一个或多个片段，请选中它们并按 Delete 键。片段将被移除。如果您 `P725`
- **空格 键** — 完成修剪后，按空格键会启动环绕当前选择播放，以便您查看修剪后的播放效果。在动态修剪模 `P731`
- **Delete 键** — 提示： 或者，您可以在时间线中选择一个或多个转场，然后按 Delete 键将其移除。 `P759`
- **Delete 键** — • 删除转场：在时间线中选择一个转场，然后按 Delete 键。 快编 `P761`
- **Command-N** — 打开剪辑页面，选择文件 > 新建时间线（Command‑N）。 `P801`
- **Command-/** — • 循环播放：启用或禁用循环播放。也可通过「播放 > 循环/取消循环」命令（Command‑/）控制 `P816`
- **K 键** — 要在剪辑页面时间线上使用「快速审阅」功能，请选择「播放 > 快速审阅」。按 K 键或空格键将使 `P819`
- **Return 键** — 中的片段并按 Return 键将其打开到源检视器中。 `P819`
- **Command-C** — 和「粘贴」命令来复制和粘贴时码值。您也可以点击时码字段并使用常规的复制（Command‑C）和 `P820`
- **Command-V** — 粘贴（Command‑V）键盘命令。这甚至可以跨页面使用。您粘贴的时码值必须是有效的时码，例如， `P820`
- **Shift-S** — 选择时间线 > 音频搓擦 (Shift‑S) `P826`
- **S 键** — 是滑动模式，用于微移、时码输入调整或动态修剪（通过按 S 键设置）。 `P827`
- **Command-Shift-W** — • 选择工作区 > 视频示波器 > 开启/关闭 (Command‑Shift‑W)，将视频示波器打开为浮动窗口。 `P830`
- **Command-Z** — • 选择编辑 > 撤销（Command‑Z）以撤销上一步更改。 `P834`
- **Shift-Command-Z** — • 选择编辑 > 重做（Shift‑Command‑Z）以重做到下一步更改。 `P834`
- **Command-N** — • 选择文件 > 新建时间线（Command‑N）。 `P839`
- **Option-Command-S** — • 选择文件 > 创建时间线备份（Option‑Command‑S）。 `P844`
- **回车 键** — 您想要的名称并按回车键。 `P849`
- **N 键** — 要打开和关闭片段及播放头吸附： 点按工具栏中的吸附按钮，或按 N 键。当吸附打开时， `P852`
- **N 键** — 使用指针拖动播放头时，您也可以按 N 键临时打开或关闭吸附（操作完成时，吸附将恢 `P852`
- **Q 键** — 哪个检视器中。源检视器中的时间线是只读的。您可以通过点按标签、按 Q 键或点按相应的检视器， `P858`
- **Q 键** — • 要在源检视器和时间线检视器之间切换，请按 Q 键。 `P858`
- **Return 键** — • 将所选片段或时间线打开到源检视器中：按 Return 键。将片段打开到源检视器后，您可以使用传 `P867`
- **Shift-S** — • 选择时间线 > 音频擦洗 (Shift‑S)。 `P876`
- **Enter 键** — 输入的数字会出现在处于焦点的检视器右上角的时码字段中。输入完成后，按 Enter 键执行时码命令。 `P877`
- **Return 键** — 绝对时码只需输入一个时码值即可。只要您按 Return 键时未选中任何片段或编辑点，播放头就会移动 `P878`
- **Command-C** — 贴命令来复制和粘贴时码值。您也可以点击时码字段并使用常规的复制 (Command‑C) 和粘贴 `P879`
- **Command-V** — (Command‑V) 键盘命令。这甚至可以跨页面使用。您粘贴的时码值必须是有效的时码，例如您不能将 `P879`
- **I 键** — 标记简单入点和出点： 使用传输控制按钮右侧的入点和出点按钮，或按 I 键或 O 键。 `P882`
- **Command-Option-I** — Shift‑Option‑I）/ 标记音频入点（Command‑Option‑I）/ 标记视频出点（Shift‑Option‑O）/ 标 `P882`
- **Shift-Option-O** — Shift‑Option‑I）/ 标记音频入点（Command‑Option‑I）/ 标记视频出点（Shift‑Option‑O）/ 标 `P882`
- **Command-Option-O** — 记音频出点（Command‑Option‑O）。 `P882`
- **Return 键** — 使用源检视器： 在媒体池中双击片段，或在媒体池中选中片段后按 Return 键或 Enter 键，即可在 `P891`
- **I 键** — 放头，并使用传输控制按钮右侧的入点和出点按钮，或按 I 键或 O 键，放置入点和出点。如果您 `P891`
- **Command-T** — 点中减去余量（使用用户偏好设置剪辑面板中的默认余量长度）。结合添加转场工具（Command‑T） `P895`
- **Command-T** — （Command‑T）。这样将一次性对所有片段应用默认转场。 `P895`
- **Return 键** — 按 Return 键或 Enter 键，将所选片段打开到源检视器中。 `P896`
- **Shift-F10** — 置顶（F12）、波纹覆盖（Shift‑F10）、适配填充（Shift‑F11）或追加到时间线末尾（Shift‑F12）到时间线中。 `P898`
- **Shift-F11** — 置顶（F12）、波纹覆盖（Shift‑F10）、适配填充（Shift‑F11）或追加到时间线末尾（Shift‑F12）到时间线中。 `P898`
- **Shift-F12** — 置顶（F12）、波纹覆盖（Shift‑F10）、适配填充（Shift‑F11）或追加到时间线末尾（Shift‑F12）到时间线中。 `P898`
- **Option-Shift-V** — 择播放头下方的所有片段（Option‑Shift‑V），一次性选中播放头下方的所有片段。 `P899`
- **Y 键** — 按 Y 键（时间线 > 向前选择片段 > 在当前轨道上），即可选中该片段以及时间线同一轨道中其右 `P900`
- **U 键** — 可以按 U 键，在所有选中的剪辑点之间切换波纹入点编辑、波纹出点编辑和卷动编辑。 `P901`
- **V 键** — 按 V 键选择已启用目标轨道选择器的最低轨道上距播放头最近的剪辑点。如果多个轨道上存在重 `P902`
- **U 键** — 间切换，请按 U 键。 `P902`
- **Delete 键** — Command 单击选择您要移除的片段，然后按 Delete 键（或右键单击所选内容并选择删除）。 `P909`
- **Delete 键** — 除的媒体范围，然后关闭您想要保留媒体的任何轨道的目标轨道选择器控件，并按 Delete 键（或右 `P909`
- **Command-C** — 右键点击该空隙并选择复制 (Command‑C)。 `P911`
- **Command-V** — 右键点击并选择粘贴 (Command‑V)。 `P911`
- **Delete 键** — 按 Delete 键关闭该空隙。已启用同步锁的轨道上位于其右侧的所有片段都会向左波纹以关闭该空隙。 `P912`
- **Option-Period** — • 向前一帧 (Option‑Period) `P917`
- **Option-Comma** — • 向后一帧 (Option‑Comma) `P917`
- **Return 键** — 自动选择下一个未排序片段： 勾选此复选框后，当您在输入元数据字段后按 Return 键时，会选中 `P936`
- **A 键** — 按 A 键选择选择模式。 `P945`
- **E 键** — 并按 E 键执行「延长」剪辑。在选择模式下，这会覆盖相邻片段。 `P945`
- **B 键** — • 拆分多个片段： 点击剃刀编辑模式按钮（或按 B 键），然后在时间线中点击片段，即可拆分任意数量的片段。 `P950`
- **Command-X** — • 剪切一个或多个片段并留下空隙： 进行选择，然后选择编辑 > 剪切（Command‑X）。所选片段 `P953`
- **Command-Shift-X** — 纹剪切（Command‑Shift‑X）。所选片段会从时间线中移除并存储在内存中以便粘贴。所有启用了 `P953`
- **Command-C** — • 复制一个或多个片段： 进行选择，然后选择编辑 > 复制（Command‑C）。所选片段会保留在时 `P953`
- **Command-V** — （Command‑V）。默认情况下，每个复制的片段会粘贴到其复制时所在的同一轨道。 `P953`
- **Command-V** — Option 单击该轨道的自动选择控件以单独选中该轨道，然后选择编辑 > 粘贴（Command‑V）。 `P953`
- **Command-Shift-V** — 另一个粘贴命令是编辑 > 粘贴插入（Command‑Shift‑V），它让您可以通过插入编辑粘贴剪切或复制 `P953`
- **Command-\** — • 新建剪切点：将时间线指示器放在剪切点处，然后选择时间线 > 拆分片段（Command‑\）。 `P964`
- **Q 键** — • 设置入点或出点：通过单击或按 Q 键选择时间线或时间线检视器，然后使用传输控制、慢搜条或 `P970`
- **I 键** — 控制面板按钮移动播放头，按 I 键设置入点，或按 O 键设置出点。 `P970`
- **O 键** — 控制面板按钮移动播放头，按 I 键设置入点，或按 O 键设置出点。 `P970`
- **X 键** — 按 X 键可自动设置与目标片段首帧和末匹配帧的入点和出点。 `P972`
- **F10 键** — 要进行剪辑，请点击工具栏中的“覆盖片段”按钮，按 F10 键，或将片段拖到时间线检视器中相应的叠加区域上。 `P977`
- **Option-Shift-Q** — 剪辑菜单中的一项设置“剪辑后将焦点切换到时间线”（Option‑Shift‑Q），可让您设定 DaVinci `P980`
- **F10 键** — 要执行剪辑，请选择 编辑 > 覆盖，点击工具栏中的覆盖片段按钮，按 F10 键，或将片段拖到时间 `P981`
- **F9 键** — • 选择 编辑 > 插入，点击工具栏中的插入剪辑按钮，按 F9 键，或将任意片段拖到时间线检视器中 `P982`
- **X 键** — • 点击该句中的任意词，然后按 X 键以选中整句，并以青色高亮显示。 `P999`
- **X 键** — • 单击该句中的任意词并按 X 键，以选中整句并将其以青色高亮显示。 `P1005`
- **Delete 键** — 按 Delete 键。您也可以双击标记打开其对话框，然后点击删除按钮。 `P1018`
- **Backspace 键** — • 移除片段的所有标记：选择一个或多个带要移除标记的片段，然后按 Backspace 键，或点击工具 `P1018`
- **Command-M** — • 双击该标记，或选择「修改标记」(Command‑M)，即可编辑该标记。 `P1022`
- **Shift-Up Arrow** — • 将播放头移到下一个或上一个标记： 选择「播放 > 上一个标记」(Shift‑Up Arrow) 或「下一个标 `P1030`
- **Shift-Down Arrow** — 记」(Shift‑Down Arrow)。 `P1030`
- **F 键** — 按 F 键，或点按时间线检视器右下角的匹配帧按钮（位于入点和出点按钮的左侧）。 `P1032`
- **F 键** — 点按源检视器右下角的匹配帧按钮（位于入点和出点按钮的左侧），或按 F 键。 `P1032`
- **Return 键** — • 将播放头移到时间线中的某个片段，按 Shift‑V 将其选中，然后按 Return 键。 `P1033`
- **Option-Shift-左箭头** — • 选择 剪辑 > 多机位 > 上一页（Option‑Shift‑左箭头）或下一页（Option‑Shift‑右箭头）。 `P1042`
- **Option-Shift-右箭头** — • 选择 剪辑 > 多机位 > 上一页（Option‑Shift‑左箭头）或下一页（Option‑Shift‑右箭头）。 `P1042`
- **Option-Shift-[** — 视频还是仅切换音频。您也可以选择 剪辑 > 多机位 > 视频和音频（Option‑Shift‑[）、仅视频（ `P1042`
- **Option-Shift-\** — Option‑Shift‑]）或仅音频（Option‑Shift‑\）。 `P1042`
- **Delete 键** — 向单个片段添加穿插剪辑，因此您可以选择任意剪辑点并按 Delete 键将其删除。 `P1044`
- **Return 键** — 输入新名称，然后按 Return 键确认更改。 `P1054`
- **Command-Page Up** — 线内容会很有用。要做到这一点，请选择时间线 > 交换时间线与源检视器（Command‑Page Up）。 `P1056`
- **T 键** — • 点击修剪编辑按钮，或按 T 键。 `P1061`
- **S 键** — 滑移片段： 按 Shift‑V 选择片段，然后按 S 键切换到滑移模式（再次按 S 可切换回滑动模式），并 `P1069`
- **Enter 键** — 您输入的数字会显示在获得焦点的检视器右上角的时码字段中。输入完成后，按 Enter 键执行时码命 `P1071`
- **Return 键** — 绝对时码只需直接输入一个时码值即可。只要您按 Return 键时没有选中任何片段或编辑点，播放 `P1071`
- **Shift-V** — 这些命令类似于同时使用编辑选择（V）或片段选择（Shift‑V）键盘快捷键，以及选择工具（A）或 `P1072`
- **Shift-Command-X** — • 波纹切割片段（Shift‑Command‑X） 剪辑 `P1074`
- **Shift-[** — • 在修剪模式下使用扩展编辑（E）、修剪开始（Shift‑[）或修剪结束（Shift‑]）命令 `P1074`
- **Shift-]** — • 在修剪模式下使用扩展编辑（E）、修剪开始（Shift‑[）或修剪结束（Shift‑]）命令 `P1074`
- **Command-Shift-[** — • 在任何模式下使用波纹开始（Command‑Shift‑[）或波纹结束（Command‑Shift‑]）命令 `P1074`
- **Command-Shift-]** — • 在任何模式下使用波纹开始（Command‑Shift‑[）或波纹结束（Command‑Shift‑]）命令 `P1074`
- **Shift-F10** — • 执行插入编辑（F9）或波纹覆盖编辑（Shift‑F10） `P1074`
- **U 键** — 按 U 键选择每个所选编辑点的入点半部。 `P1080`
- **空格 键** — 完成后，按空格键或 K 键停止播放。 `P1083`
- **W 键** — • 开启动态模式： 如果您想进行更细致的工作，可以按 W 键启用动态模式（或选择修剪 > 动态 `P1084`
- **S 键** — 时间线中选中了一个或多个片段，可以移动或滑移片段。您可以通过按 S 键，或右键单击工具栏 `P1085`
- **S 键** — 中选中了一个或多个片段，可以滑动或滑移片段。您可以通过按 S 键，或右键单击工具栏中的动 `P1086`
- **空格 键** — 修剪完成后，按空格键会启动环绕播放当前选择，以便您查看该修剪的播放效果。 `P1086`
- **Shift-[** — 修剪 > 修剪开头（Shift‑[）和修剪结尾（Shift‑]）命令可让您移动与播放头相交的所有片段的入点或 `P1086`
- **Shift-]** — 修剪 > 修剪开头（Shift‑[）和修剪结尾（Shift‑]）命令可让您移动与播放头相交的所有片段的入点或 `P1086`
- **Command-Shift-[** — • 将开头波纹到播放头（Command‑Shift‑[） `P1088`
- **Command-Shift-]** — • 将结尾波纹到播放头（Command‑Shift‑]） `P1088`
- **E 键** — 在每条轨道上选择任意组合的入点或出点各一个，然后按 E 键将这些编辑点移动到播放头的当前位置。 `P1088`
- **Backspace 键** — 按 Backspace 键删除该段音频并留下间隙，或按 Forward Delete 键删除该段音频并以波纹方式 `P1105`
- **N 键** — 这种情况下，按 N 键关闭吸附即可解决此问题。 `P1107`
- **Command-X** — • 剪切或复制并粘贴一个或多个关键帧：选择一组关键帧，并使用剪切（Command‑X）或复制 `P1113`
- **Command-C** — （Command‑C）快捷键。然后，将播放头移到您希望第一个复制的关键帧开始的位置，并按粘 `P1113`
- **Command-V** — 贴（Command‑V）。 `P1113`
- **Shift-T** — 「时间线 > 仅添加音频转场」（Shift‑T）。 `P1114`
- **Option-V** — 选择“编辑 > 粘贴属性”（Option‑V），或右键点按所选片段之一，在上下文菜单中选择“粘贴属性”。 `P1155`
- **U 键** — • 使用键盘添加具有特定对齐方式的转场： 选择一个编辑，按 U 键选择该编辑的起点、中心或终点， `P1160`
- **Delete 键** — • 移除转场： 在时间线中选择一个转场并按 Delete 键。或者，在时间线中右键点按某个转场，从上 `P1160`
- **Windows 键** — 在 macOS 上按 Control‑Command‑空格键，在 Windows 上按 Windows 键‑.（句点），即可将表情 `P1182`
- **Command-R** — • 选择一个片段，然后选择片段 > 变速控件（Command‑R）。 `P1218`
- **Escape 键** — • 按 Escape 键。 `P1222`
- **Option-Command-L** — 选择 片段 > 链接片段（Option‑Command‑L）。会出现一个链接图标，表示这些字幕片段已链接 `P1243`
- **Command-[** — • 选择标记 > 添加关键帧（Command‑[）。 `P1254`
- **Option-]** — • 选择标记 > 删除关键帧（Option‑]）。 `P1254`
- **Command-X** — • 要剪切或复制并粘贴一个或多个关键帧：选定一组关键帧，并使用剪切（Command‑X）或复制 `P1258`
- **Shift-V** — 示、矢量示波器或波形图示波器。交换选项（Shift‑V）让您将检视器中所显示的内容与附件窗格中 `P1292`

## Fusion（48 条）

- **V 键** — 以快速平移到合成的不同部分。您可以使用左下角的手柄调整导航器的大小，还可以按 V 键，或右键 `P1309`
- **Command-Z** — • 选择 编辑 > 撤销 (Command‑Z) 以撤销上一步更改。 `P1330`
- **Shift-Command-Z** — • 选择 编辑 > 重做 (Shift‑Command‑Z) 以重做下一步更改。 `P1330`
- **Option-F** — 您可以选择该节点，然后选择“片段 > 在媒体池中查找片段”(Option‑F)。 `P1341`
- **V 键** — • 按 V 键。 `P1382`
- **Return 键** — 当您选择了正确的节点后，按 Return 键（或点击确定），该节点就会被插入或添加。 `P1386`
- **Delete 键** — • 移除路由器： 选择任意路由器并按 Delete 键，或右键单击路由器并从右键菜单中选择删除。 `P1406`
- **Command-C** — • 从编辑菜单中选择编辑 > 复制（Command‑C）。 `P1408`
- **Command-X** — • 从编辑菜单中选择编辑 > 剪切（Command‑X）。 `P1408`
- **Command-V** — 编辑 > 粘贴（Command‑V）。 `P1408`
- **Command-V** — 编辑 > 粘贴（Command‑V），或在节点编辑器中任意位置右键单击并从右键菜单中选择粘贴。 `P1409`
- **Command-C** — 选择包含您想复制的设置的节点，然后从编辑菜单中选择复制（Command‑C）。 `P1409`
- **Command-C** — 选择要实例化的节点，并将其复制（Command‑C）。 `P1413`
- **Return 键** — 选择工具」窗口中出现 Sticky Note 时按 Return 键。或者，您可以打开特效库，展开 工具 > 节点编 `P1417`
- **Delete 键** — 签，然后按 Delete 键。 Fusion `P1417`
- **Return 键** — 当「选择工具」窗口中出现 Underlay Box 时按 Return 键。或者，您可以打开特效库，展开 工具 > `P1417`
- **Return 键** — 格键，输入 under，当「选择工具」窗口中出现 Underlay Box 时按 Return 键。或者，您可以打开 `P1418`
- **Delete 键** — • 删除衬底框及其中的所有节点： 选中衬底框并按 Delete 键，即可同时删除衬底框和其中的所有节 `P1418`
- **Command-P** — • 直通：（Command‑P）与检查器中用于关闭和打开节点的切换开关相同。禁用节点会被忽略， `P1424`
- **Command-L** — • 已锁定：（Command‑L）与检查器中的锁定按钮相同，可防止在检查器中编辑节点。默认关闭。 `P1424`
- **Command-U** — • 更新：（Command‑U）默认开启。启用该选项时，对节点的所有更改都会使其重新渲染。禁用 `P1424`
- **Command-G** — 右键点击所选节点之一，从上下文菜单中选择 Group（Command‑G）。 `P1427`
- **Command-L** — 锁定检视器 （Command‑L） ........................................................................… `P1451`
- **空格 键** — • 按空格键开始或停止播放。 `P1458`
- **空格 键** — • 按住 Shift 并按空格键可反向播放。 `P1458`
- **V 键** — • 单击一个检视器，然后按 V 键。 `P1464`
- **Command-L** — 锁定检视器（Command‑L） `P1491`
- **Return 键** — 双击您要拾取连接到另一个参数的参数字段，输入 =，然后按 Return 键。 `P1512`
- **Command-R** — 缩放到矩形按钮（Command‑R）允许您在想要居中并缩放的图表区域周围绘制一个边界框。 `P1539`
- **Command-F** — • 选择缩放 > 缩放适配（Command‑F），使所有活动的样条适配到图表区域中。 `P1540`
- **Command-R** — • 选择缩放 > 缩放到矩形（Command‑R），在您想要居中并缩放的图表区域周围绘制一个边界框。 `P1540`
- **Tab 键** — 按 Tab 键或单击关闭按钮以关闭标记列表。 `P1542`
- **Command-C** — 图表的上下文菜单中有两个用于复制关键帧的选项。选择复制点（Command‑C）会复制所有选中的 `P1548`
- **V 键** — 钮，或右键单击并从上下文菜单中选择“反转”，或按 V 键。该组点会立即在图表中水平镜像。反转 `P1557`
- **Tab 键** — • 自动推进控件： 如果启用了自动推进控件复选框，按 Tab 键和 Return/Enter 键将使键盘焦点前 `P1627`
- **Shift-C** — 单击追加工具栏按钮（Shift‑C） `P1734`
- **Shift-I** — 插入修改工具栏按钮（Shift‑I） `P1735`
- **Shift-D** — 绘制追加工具栏按钮（Shift‑D） `P1735`
- **Shift-M** — 仅修改工具栏按钮（Shift‑M） `P1735`
- **Shift-N** — 完成工具栏按钮（Shift‑N） `P1736`
- **Shift-O** — 关闭工具栏按钮（Shift‑O） `P1736`
- **Shift-S** — 工具栏中的平滑按钮（Shift‑S） `P1737`
- **Shift-L** — 工具栏中的直线按钮（Shift‑L） `P1738`
- **Return 键** — 只需双击跟踪器列表中该跟踪器的默认名称，输入新名称，然后按 Return 键即可。 `P1782`
- **Escape 键** — • 按 Escape 键。 `P1785`
- **Option 键** — 按住鼠标中键和左键并拖动可缩放检视器，或用中键拖动以平移检视器，或按 Option 键加中键拖 `P1833`
- **Windows 键** — Windows 上按 Windows 键‑.（句点）。在编辑框中右键单击会显示自定义上下文菜单。有关这些修 `P2383`
- **Delete 键** — 播放时，可以在屏幕上看到并选择虚假轨迹，然后按 Delete 键将其删除。这一过程需要经验丰富的眼 `P2819`

## 调色（70 条）

- **Command-Z** — • 选择编辑 > 撤销（Command‑Z）以撤销上一步更改。 `P3062`
- **Shift-Command-Z** — • 选择编辑 > 重做（Shift‑Command‑Z）以重做到下一步更改。 `P3062`
- **Shift-D** — 色和特效，以提升低配置电脑上的播放性能。单击该按钮（Shift‑D）可停用或重新启用调色和特效， `P3072`
- **Option-Shift-Z** — • 从缩放下拉菜单中选择 100%（实际大小）（Option‑Shift‑Z），该菜单位于 `P3076`
- **Shift-Z** — • 选择视图 > 缩放 > 缩放至适合（Shift‑Z）。 `P3076`
- **Option-F** — 选择工作区 > 检视器模式 > 增强检视器模式（Option‑F），即可将检视器扩展为增强查看模式。 `P3079`
- **Option-F** — 器上方的整个屏幕区域。要退出增强查看模式，请再次按（Option‑F）。 `P3079`
- **Option-Command-G** — • 选择调色 > 静帧 > 抓取静帧（Option‑Command‑G）。 `P3084`
- **Command-W** — • 选择调色 > 静帧 > 播放静帧（Command‑W），或右键单击检视器并选择「切换划像」。 `P3085`
- **Option-W** — • 反转划像：反转划像的每一半（Option‑W）。 `P3086`
- **Command-Shift-W** — • 选择工作区 > 视频示波器 > 开/关（Command‑Shift‑W），将视频示波器打开为一个浮动窗口。 `P3093`
- **Delete 键** — 法是，在迷你时间线中选择该标记，然后按 Delete 键。 `P3114`
- **Option-Shift-C** — 调色• 选择 调色 > 自动调色 (Option‑Shift‑C)。 `P3129`
- **Option-Shift-H** — • 从检视器选项菜单中选择高亮显示 > 高亮显示黑白（Option‑Shift‑H）。 `P3252`
- **Option-C** — 选择调色 > 节点 > 添加串联节点 + CPW（Option‑C）可创建带圆形窗口的新串联节点。 `P3268`
- **Option-Q** — 选择调色 > 节点 > 添加串联节点 + LPW（Option‑Q）可创建带线性窗口的新串联节点。 `P3268`
- **Option-G** — 选择调色 > 节点 > 添加串联节点 + PPW（Option‑G）可创建带多边形窗口的新串联节点。 `P3268`
- **Option-B** — 选择调色 > 节点 > 添加串联节点 + PCW（Option‑B）可创建带曲线窗口的新串联节点。 `P3268`
- **Backspace 键** — • 删除控制点：按 Backspace 键。 `P3275`
- **Escape 键** — 完成后，按 Escape 键取消选择控制点。 `P3275`
- **Command-T** — • 向前跟踪（Command‑T）：从播放头当前位置向前将窗口跟踪到某个特征，直至片段的最后一帧。 `P3308`
- **Option-T** — • 反向跟踪（Option‑T）：从播放头当前位置向后将窗口跟踪到某个特征，直至片段的第一帧。 `P3308`
- **Command-Option-T** — • 停止跟踪（Command‑Option‑T）：中断任何跟踪。这可用于取消出错的长时跟踪（控制面板 `P3309`
- **Option-右箭头** — • 向前一帧（Option‑右箭头）：从当前位置向前一帧将窗口跟踪到某个特征。 `P3309`
- **Option-左箭头** — • 反向一帧（Option‑左箭头）：从当前位置向后一帧将窗口跟踪到某个特征。 `P3309`
- **Option-Command-G** — • 选择 调色 > 静帧 > 抓取静帧（Option‑Command‑G）。 `P3333`
- **Option-Command-N** — • 从 调色 > 静帧 菜单中，选择下一个静帧（Option‑Command‑N）或上一个静帧 `P3336`
- **Option-Command-B** — （Option‑Command‑B）以在画廊中选择一个静帧，然后选择划像开/关（Command‑W）来播放它。 `P3336`
- **Command-W** — （Option‑Command‑B）以在画廊中选择一个静帧，然后选择划像开/关（Command‑W）来播放它。 `P3336`
- **Option-W** — • 从检视器的选项菜单中选择反转划像（Option‑W）。 `P3336`
- **Option-1 至 8** — • 选择 调色 > 记忆 > 保存记忆 A–H（Option‑1 至 8）。 `P3348`
- **Command-N** — 打开剪辑页面，选择文件 > 新建时间线（Command‑N）。 `P3354`
- **Command-Y** — • 选择调色 > 调色版本 > 添加（Command‑Y）。 `P3355`
- **Option-1** — 选择调色 > 记忆 > 保存记忆 A（Option‑1）。您可以使用任意记忆，但在此示例中记忆 A 比较方便。 `P3359`
- **Command-U** — 选择调色 > 调色版本 > 默认（Command‑U）。 `P3359`
- **Command-1** — 选择调色 > 记忆 > 加载记忆 A（Command‑1），将保存的记忆应用到默认版本。 `P3359`
- **Command-U** — • 选择调色 > 调色版本 > 默认（Command‑U）。 `P3359`
- **Shift-Option-V** — 编辑 > 粘贴数值（Shift‑Option‑V）命令让您仅将特定参数的数值粘贴到另一个片段的节点中。 `P3362`
- **Shift-Option-V** — 选择编辑 > 粘贴数值（Shift‑Option‑V）来粘贴该数值。 `P3362`
- **Option-1–8** — • 选择调色 > 记忆 > 保存记忆 A–H（Option‑1–8）。 `P3365`
- **Command-1–8** — • 选择调色 > 记忆 > 加载记忆 A–H（Command‑1–8）。 `P3365`
- **Option-Shift-P** — 选择调色 > 预览记忆（Option‑Shift‑P）。 `P3365`
- **Command-1–8** — • 选择记忆 > 加载 > 记忆 A–H（Command‑1–8）。现在预览的是所选调色或记忆。 `P3365`
- **Option-Shift-P** — • 如果您不喜欢该效果，再次选择调色 > 预览记忆（Option‑Shift‑P）会将片段恢复到原始调色。 `P3365`
- **Command-C** — • 选择一个带有您想复制的设置的节点，然后选择编辑 > 复制（Command‑C）。接着，在当前 `P3367`
- **Command-V** — 辑 > 粘贴（Command‑V）粘贴您所复制的设置。这些粘贴的节点设置会覆盖该节点之前使用的 `P3367`
- **Option-V** — 您可以通过选择编辑 > 粘贴属性（Option‑V）复制一个节点的设置，并将其中的一部分设置粘贴到另 `P3368`
- **H 键** — • 按 H 键可在节点编辑器中的选择模式和平移模式之间切换。 `P3388`
- **Return 键** — 输入您想要的标签文本，然后按 Return 键。 `P3390`
- **Command-D** — • 选中单个节点，然后选择 调色 > 节点 > 启用/禁用所选节点（Command‑D）。 `P3392`
- **Option-D** — • 选择 调色 > 节点 > 启用/禁用所有节点（Option‑D），即可切换所有节点的开关状态。 `P3392`
- **Shift-home** — • 重置所选节点：按 (Shift‑home)。 `P3393`
- **Command-home** — • 重置所有调色和节点：按 (Command‑home)。 `P3393`
- **Option-Shift-O** — 色 > 原始记忆（Option‑Shift‑O）即可访问。如果您做了一系列后来感到后悔的更改，它非常适合 `P3394`
- **Option-C** — • 节点 + CPW： 圆形窗口（Option‑C） `P3395`
- **Option-Q** — • 节点 + LPW： 线性窗口（Option‑Q） `P3395`
- **Option-G** — • 节点 + PPW： 多边形窗口（Option‑G） `P3395`
- **Option-B** — • 节点 + PCW： PowerCurve 窗口（Option‑B） `P3395`
- **Command-C** — • 选中包含您要复制的设置的节点，然后选择 编辑 > 复制（Command‑C）。接着，在当前调色或 `P3397`
- **Return 键** — 入新标签。完成后按 Return 键。 调色 `P3402`
- **Option-O** — • 选择节点 > 添加外部（Option‑O）。 `P3417`
- **Option-Y** — • 要添加预制的拆分器/合并器节点结构：选择调色 > 节点 > 添加拆分器/合并器节点 (Option‑Y)。 `P3441`
- **Option-L** — 通过选择节点 > 添加图层 (Option‑L) 创建图层混合器节点。 `P3444`
- **Command-[** — • 选择标记 > 添加关键帧（Command‑[）。 `P3456`
- **Command-]** — • 选择标记 > 制作静态关键帧（Command‑]）。 `P3457`
- **Option-]** — • 将播放头移到您想删除的关键帧上，然后选择标记 > 删除关键帧（Option‑]）。播放头位置处的 `P3463`
- **Command-C** — 选择一个要从中复制关键帧的节点，然后选择编辑 > 复制（Command‑C）。 `P3464`
- **Command-V** — • 要复制所有关键帧，请选择另一个要粘贴关键帧和其他调整的节点，然后选择编辑 > 粘贴（Command‑V）。 `P3464`
- **Option-V** — • 如果您只想从特定关键帧轨道复制关键帧信息，请选择编辑 > 粘贴属性（Option‑V），然后点 `P3464`
- **Shift-F** — 模式时，它仍会保持可见。选择工作区 > 检视器模式 > 全屏检视器（Shift‑F）即可进入该模式。 `P3491`

## Fairlight（16 条）

- **Command-F** — • Fairlight 页面有影院模式观看（Command‑F），也可以通过“工作区”菜单使用：工作区 > 检 `P3772`
- **Command-Z** — • 选择编辑 > 撤销（Command‑Z）以撤销上一项更改。 `P3798`
- **Shift-Command-Z** — • 选择编辑 > 重做（Shift‑Command‑Z）以重做到下一项更改。 `P3798`
- **空格 键** — • 播放： 向前播放。等同于在播放停止时按空格键或 L 键。 `P3823`
- **空格 键** — • 停止： 停止播放。等同于在播放进行时按空格键或 K 键。 `P3823`
- **Enter 键** — 您输入的数字会显示在检视器右上角获得焦点的时码字段中。输入完成后，按 Enter 键执行时码命令。 `P3825`
- **空格 键** — • 按空格键。 `P3836`
- **回车 键** — 完成后按回车键。 `P3848`
- **Shift-[** — 修剪 > 修剪开始（Shift‑[）和修剪结束（Shift‑]）命令让您可以将与播放头相交的所有片段的入点或 `P3876`
- **Shift-]** — 修剪 > 修剪开始（Shift‑[）和修剪结束（Shift‑]）命令让您可以将与播放头相交的所有片段的入点或 `P3876`
- **Shift-Command-T** — 修剪 > 修剪至所选（Shift‑Command‑T）命令会同时修剪一个或多个片段选择范围之外的头和尾，使 `P3877`
- **Delete 键** — 在指针模式或范围模式下，您可以通过按 Delete 键，或右键点击片段并从上下文菜单中选择删除所选， `P3880`
- **Shift-T** — 添加音频转场 (Shift‑T)。 FAIRLIGHT `P3890`
- **Return 键** — 勾选此框后，在填写完某个元数据字段后按 Return 键，会选择下一个媒体池片段，并将光标置于同一 `P3916`
- **空格 键** — 形下方传输控件中的播放按钮，或按空格键来收听音频。如果您点击任意波形内的任意位置，播 `P3918`
- **Command-F** — 全屏观看节目画面会有所帮助。您现在可以通过选择工作区 > 检视器模式 > 影院模式 (Command‑F) `P4061`

## 交付（3 条）

- **I 键** — • 将播放头定位在时间线或检视器中，然后按 I 键，或右键单击时间线标尺并选择标记入点。 `P4129`
- **O 键** — • 将播放头定位在时间线或检视器中，然后按 O 键，或右键单击时间线标尺并选择标记出点。 `P4129`
- **Shift-Command-O** — 选择文件 > 导出 > 时间线（Shift‑Command‑O）。 `P4160`

## 照片 / 其他（3 条）

- **Ctrl-G** — 在远程客户端的工作站上选择 工作区 > 远程调色（Ctrl‑G）。会显示一个窗口，其中包含用于输入 `P4257`
- **Shift-UpArrow** — 个标记（Shift‑UpArrow）和下一个标记（Shift‑DownArrow）命令。这让您可以直接在 Dropbox `P4324`
- **Shift-DownArrow** — 个标记（Shift‑UpArrow）和下一个标记（Shift‑DownArrow）命令。这让您可以直接在 Dropbox `P4324`

