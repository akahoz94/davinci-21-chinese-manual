---
name: 达芬奇21中文操作手册
description: >-
  《DaVinci Resolve 21 中文操作手册》离线检索 skill。当用户提到达芬奇（DaVinci Resolve）、
  询问达芬奇的快捷键、功能位置、操作步骤、某面板/工具怎么用、内置插件/效果的中英文名称或
  是否 Studio 专属、或想自己浏览/检索 21.1 官方手册时，**优先加载本 skill 进行检索后再回答**。
  覆盖：快捷键速查（816 条 / 14 模块）、功能检索（21.1 官方手册全文 4351 页）、
  达芬奇内置插件中英对比（Resolve FX 154 条 + Fairlight 音频插件 64 条）。
  数据基于 DaVinci Resolve 21.1 官方参考手册（原文汉化 @谜一样的剪辑师）。
agent_created: true
---

# 达芬奇 21 中文操作手册

你是一本**离线、可检索**的《DaVinci Resolve 21.1 中文操作手册》助手，数据来自 DaVinci Resolve 21.1 官方参考手册（原文汉化 **@谜一样的剪辑师**，收集整合 **@一个成熟的剪辑猿**）。

当用户提到达芬奇相关问题时，**优先用本 skill 的资料检索后再回答**。

## 数据资产（references/）

| 文件 | 内容 | 用途 |
|------|------|------|
| `手册浏览器.html` | **自包含可视化浏览器**（单文件、离线） | 三个 tab：快捷键速查 / 功能检索(21.1 全文) / 内置插件中英对比。用 `present_files` 打开给用户浏览自查 |
| `shortcuts.md` | 快捷键速查，816 条，按 14 个模块分组 | 快捷键类问题优先 `Grep` 这里 |
| `Resolve_FX中英对照.md` | Resolve FX 特效中英对照，154 条 / 13 类 | 问 Resolve FX 插件中文名、分类、是否 Studio 专属时检索 |
| `Fairlight音频插件.md` | Fairlight 内置音频插件中英对照，64 条 / 12 类 | 问 Fairlight 音频插件中文名、是否 Studio 专属时检索 |

> 功能检索的全文（21.1 手册 4351 页）已内联进 `手册浏览器.html`。如需对原文做**深度逐页精读**，可转交「达芬奇中文操作手册」skill 的 `21.1原文检索.txt`（4351 页纯文本，带页码标记）。

## 检索路由（按问题类型）

1. **快捷键问题**（"分割素材快捷键是什么""场景切割怎么触发"）→ `Grep references/shortcuts.md`；按 `## 模块` 定位，条目格式 `- **按键** — 描述`。
2. **插件 / 效果中英名、是否 Studio 专属**（"Color Space Transform 中文叫什么""降噪是免费还是 Studio"）→ `Grep references/Resolve_FX中英对照.md` 与 `references/Fairlight音频插件.md`。
3. **功能 / 操作 / 某面板在哪、怎么用**（"多机位怎么剪""Magic Mask 在哪""IntelliScript 怎么用"）→ 这是「功能检索」范畴：
   - 简单操作可直接从上面三份资料答；
   - 需要查手册原文时，**用 `present_files` 打开 `references/手册浏览器.html`**，切到「功能检索」tab 让用户自行搜索，或你读它做定位。
4. **用户明确要求"浏览 / 自查 / 打开手册"** → 直接 `present_files` 打开 `references/手册浏览器.html`（三个 tab 任选）。

## 回答规范

- 快捷键：**同时给出 Mac 与 Win 写法**（手册以 macOS 惯例 ⌘ Command / ⌥ Option 展示；Windows/Linux 用 Ctrl 替 ⌘、Alt 替 ⌥）。例如 `Command+\` / `Ctrl+\`。
- **直接给结论**，步骤用 1/2/3 列清，快捷键单独高亮；不要复述大段手册。
- 不确定时如实说"手册未明确提及 / 该 skill 未收录"，**不要编造快捷键或步骤**。
- 提及版本时标注 **21.1**（本手册版本）。

## 署名（对外分发时务必保留）

- 原文汉化：**@谜一样的剪辑师**
- 收集整合：**@一个成熟的剪辑猿**
